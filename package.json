{
    "name": "actuary",
    "main": "index.js",
    "type": "module",
    "license": "GPL-3.0-or-later",
    "scripts": {
        "test": "jest",
        "dist": "ncc build -o dist src/main.js"
    },
    "jest": {
        "verbose": true,
        "testMatch": [
            "**/tests/**/*.js?(x)"
        ]
    },
    "dependencies": {
        "@actions/core": "^2.0.1",
        "@actions/exec": "^3.0.0"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.38.0",
        "eslint": "^10.0.2",
        "jest": "^30.0.2"
    }
}
